# TODO---APP-
A Flask-based Todo App with simple session login, task creation, and status tracking. Tasks cycle through pending, working, and done states, are stored in a SQLite database via SQLAlchemy, and can be cleared in bulk — a clean example of Flask blueprints in action.
